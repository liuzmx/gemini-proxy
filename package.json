{
  "name": "gemini-proxy",
  "version": "1.0.0",
  "description": "Secure Cloudflare Worker proxy for Google Generative AI",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240601.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
